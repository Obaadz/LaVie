import styled from "styled-components";

export const StyledLastBlogs = styled.section`
  font-family: "Poppins", sans-serif;

  .card {
    --bs-card-border-width: 0;
  }

  .card-header {
    background-color: unset;
    color: rgb(var(--primary-color));
  }

  h5 {
    font-weight: 600;
  }

  p {
    color: #7d7b7bc7;
  }
`;
